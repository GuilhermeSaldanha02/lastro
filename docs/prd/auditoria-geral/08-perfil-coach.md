# 08 — Perfil e Coach

## `/perfil` e folha `(.)perfil`

- [x] **PERF-01** Acessar `/perfil` via `Link` a partir de `/ajustes`. Esperado: abre como folha, avatar + nome (nome **não editável**, só leitura).
  PROVA: clique em "Editar perfil" em `/ajustes` navegou para `/perfil` mantendo `/ajustes` visível ao fundo (folha/overlay `dialog "Perfil"`). `get_page_text` mostrou "Perfil / [avatar] / qa-audit-geral2 / Trocar foto". `read_page` (filter all) no dialog: `generic "qa-audit-geral2" [ref_34]` — sem nenhum `textbox`/`input` para o nome, só texto estático. Confirmado nome não editável.
- [x] **PERF-02** Botão "Trocar foto" abre seletor de arquivo (input file oculto, `accept="image/jpeg,image/png"`).
  PROVA: `javascript_tool` no DOM da página `/perfil`: `document.querySelector('input[type=file]')` → `{"accept":"image/jpeg,image/png","hidden":"none","name":""}` (hidden via `display:none` computado). `read_page` mostra `button "Escolher foto de perfil" [ref_35] type="file"` dentro do dialog "Perfil", ao lado do botão visível "Trocar foto".
- [x] **PERF-03** Selecionar arquivo de tipo inválido (ex.: `.pdf` ou `.gif`, se der pra simular upload via ferramenta). Esperado: erro de `validarArquivoAvatar` mostrado, **sem** disparar upload de rede (checar aba de rede — nenhuma requisição de storage).
  PROVA: `file_upload` no input `ref_35` com `invalido.txt` (texto puro, criado via Bash). Após upload, `read_page` no dialog mostrou `alert "Envie uma foto em JPEG ou PNG." [ref_36]`. Screenshot confirma banner vermelho de erro abaixo do botão "Trocar foto", avatar continua o círculo vermelho original (sem mudança). `read_network_requests` (28 requisições, últimas 20 listadas): nenhuma request de storage/upload — só o `POST /api/coach` residual de teste anterior (COACH) e assets estáticos `_next`/fontes. Nenhuma requisição de rede disparada pelo upload inválido.
- [x] **PERF-04** Selecionar imagem válida. Esperado: botão vira "Enviando…" `disabled`, depois avatar atualiza na tela com a nova URL, sem precisar recarregar.
  PROVA: gerei um PNG válido 4x4 (`valido.png`, vermelho sólido) via script Python e enviei com `file_upload` no `ref_35`. `javascript_tool` logo após: `{"hasImg":true,"imgSrc":"https://tbkzcqfvafznxallyfqk.supabase.co/storage/v1/object/public/avatares/fe8983b8-dccc-4160-b010-287d3b0008a5/avatar.png?v=1786987094602"}` — URL nova com timestamp `v=` diferente da anterior (`v=1786983558478`), sem nenhum reload de página (URL da aba seguiu `/perfil`). `read_network_requests` filtrado por `supabase` mostrou o `GET` da nova imagem com `statusCode 200`. Não capturei o estado intermediário "Enviando…" no screenshot (upload de 4x4px foi rápido demais entre a chamada de upload e o screenshot seguinte), mas o resultado final (nova URL aplicada sem reload) confirma o comportamento esperado do fluxo.
- [ ] **PERF-05** `[NÃO COBERTO]` Simular falha do upload (se possível forçar, ex. arquivo válido mas por acaso a rede falha) — senão documentar como não testável nesta rodada. Esperado, se testável: mensagem de erro do `resultado.erro`, avatar não muda.
  MOTIVO: as ferramentas de navegador disponíveis (`mcp__claude-in-chrome__*`) não têm um controle de "modo offline"/throttling de rede nem forma de interceptar/derrubar a chamada ao Supabase Storage sem editar código-fonte do app (proibido para este subagente). Não há maneira confiável de forçar `resultado.erro` sem acesso a devtools de throttle de rede ou mock de API, que não estão expostos por essas ferramentas.
- [x] **PERF-06** Após um erro de arquivo (PERF-03), selecionar o **mesmo arquivo inválido de novo**. Esperado: dispara a validação de novo (o código reseta `value=""` do input pra permitir reselecionar o mesmo arquivo) — não fica "travado" achando que nada mudou.
  PROVA: `javascript_tool` checou `input[type=file].value` logo após o upload de PERF-03 → já vazio (`""`), confirmando reset. Reenviei o mesmo `invalido.txt` via `file_upload` no `ref_35`; `read_page` mostrou o mesmo `alert "Envie uma foto em JPEG ou PNG." [ref_36]` continua/reaparece (validação disparou de novo, não travou), `input.value` seguiu vazio, sem erros no console (`read_console_messages onlyErrors=true` → nenhum).
- [x] **PERF-07** Acessar `/perfil` direto pela URL (fora da folha). Esperado: página completa normal, sem "Entre para editar seu perfil" (usuário está logado).
  PROVA: `navigate` direto para `http://localhost:3002/perfil` (fora do dialog). `get_page_text`: "AJUSTES / Perfil / qa-audit-geral2 / Trocar foto" — página completa com breadcrumb "AJUSTES" e botão voltar (não é mais overlay/dialog), sem nenhum texto "Entre para editar seu perfil". Screenshot confirma layout de página cheia.
- [x] **PERF-08** Deslogar e acessar `/perfil`. Esperado: `<p className="vazio">Entre para editar seu perfil.</p>` (não redireciona — `/perfil` não está na lista de rotas privadas do proxy).
  PROVA: cliquei em "Sair" em `/ajustes`; `read_network_requests` (filtro `ajustes`) mostrou `POST /ajustes` 200 seguido de redirecionamento pra `/login` (tela de login com autofill do navegador mostrando o e-mail salvo). `navigate` direto pra `/perfil` sem sessão → `get_page_text`: "AJUSTES / Perfil / Entre para editar seu perfil." — página renderiza normalmente (URL continua `/perfil`, sem redirect pro login), confirmando que a rota não é privada no proxy.
  OBSERVAÇÃO (não é falha de PERF-08 em si): o primeiro clique no botão "Sair" via `ref` (elemento resolvido por `find`) não completou o logout — a página `/ajustes` recarregou mas continuou mostrando o usuário logado, precisei clicar de novo por coordenada de tela pra o `POST /ajustes` de fato disparar. Pode ser only uma inconsistência do `ref` ficando stale entre chamadas (comum em SPAs com re-render), não necessariamente bug do app — registrado para contexto, não bloqueia o item.

## `/coach`

- [x] **COACH-01** Acessar `/coach` pela primeira vez na sessão. Esperado: histórico vazio, mensagem inicial "Pergunte o que quiser sobre treino. Execução de exercício fica no catálogo — é escrita por pessoa, não por IA."
  PROVA: login novo + `navigate` pra `/coach`. `get_page_text`: "COACH / Tirar uma dúvida / Pergunte o que quiser sobre treino. Execução de exercício fica no catálogo — é escrita por pessoa, não por IA. / Perguntar ao coach" — texto exato bate, sem nenhuma bolha de conversa anterior. `read_page`: apenas `textbox "Perguntar…" [ref_2]` e `button "Enviar" [ref_3]`, nenhum histórico.
- [x] **COACH-02** Digitar pergunta e enviar. Esperado: fala do "dono" aparece otimisticamente antes da resposta; bolha "escrevendo…" aparece enquanto `carregando`.
  PROVA: digitei "Qual a diferença entre hipertrofia e força?" no campo (`form_input`/`type` + clique em "Enviar") — screenshot imediato após o clique mostra a bolha verde do dono já renderizada ("Qual a diferença entre hipertrofia e força?") e logo abaixo a bolha cinza "escrevendo...". `javascript_tool`: `document.querySelector('button[type=submit]').disabled` → `true` nesse instante, confirmando botão desabilitado durante `carregando`.
- [x] **COACH-03** Resposta chega com sucesso (200). Esperado: fala do "coach" aparece, rola automaticamente pro fim da conversa.
  PROVA: `read_network_requests` (filtro `coach`) mostrou `POST /api/coach` → `statusCode 200`. Screenshot seguinte mostra a bolha "COACH" com a resposta completa ("A hipertrofia é o aumento da massa..." / "A força é a capacidade..."), a tela rolou automaticamente pra baixo (cabeçalho "COACH / Tirar uma dúvida" cortado no topo, indicando scroll pro fim da conversa).
- [x] **COACH-04** Tentar enviar com campo vazio (só espaços). Esperado: botão de enviar `disabled` (trim vazio), nenhuma requisição disparada.
  PROVA: digitei 3 espaços no campo (`computer type "   "`). `javascript_tool`: `{"inputValue":"   ","btnDisabled":true}` — botão desabilitado com o campo só com espaços. `read_network_requests` (filtro `coach`) continuou com os mesmos 3 requests de antes (2 `POST /api/coach` de COACH-02/03 + 1 `GET /coach`), nenhuma requisição nova disparada.
- [x] **COACH-05** Digitar pergunta maior que `LIMITE_PERGUNTA` caracteres (checar o valor real no código/UI, ex. colar texto longo). Esperado: `maxLength` do campo bloqueia digitação além do limite, ou se passar, backend retorna 400 "Pergunta vazia ou acima de {LIMITE_PERGUNTA} caracteres."
  NOTA METODOLÓGICA: `LIMITE_PERGUNTA = 500` (achado via `grep`, `C:\lastro\src\app\api\coach\prompt.ts:37`; usado também em `coach-interativo.tsx:115` como `maxLength={LIMITE_PERGUNTA}` e no `route.ts:40` na mensagem de erro). Nesta 2ª tentativa, gerei a string de 557 caracteres (`'a'*550`+sobra) via Bash e apliquei com `form_input` (não `computer.type`), conforme orientação — evitou o travamento da 1ª tentativa.
  PROVA: `form_input` setou o `input` diretamente para 557 caracteres — isso **contorna** o atributo nativo `maxLength=500` porque atribuição programática de `.value` não é limitada pelo `maxLength` do HTML (só bloqueia digitação real via teclado; confirmado: `{"length":557,"maxLength":500}` via `javascript_tool`). Cliquei em "Enviar" mesmo assim; `read_network_requests` (filtro `api/coach`) mostrou `POST /api/coach` → `statusCode 400`. `get_page_text` confirmou a mensagem exata exibida na UI: "Pergunta vazia ou acima de 500 caracteres." — bate exatamente com o template `Pergunta vazia ou acima de ${LIMITE_PERGUNTA} caracteres.` do backend. Comportamento correto: o `maxLength` barra digitação normal do usuário e o backend é a rede de segurança real contra bypass.
- [x] **COACH-06** Deslogar e forçar `POST /api/coach` (ou recarregar `/coach` sem sessão e tentar enviar). Esperado: 401, UI mostra "Sessão expirada. Faça login novamente."
  PROVA (parte 1 — rota protegida): deslogado, `navigate` pra `/coach` redirecionou para `/login?proximo=%2Fcoach` — ou seja, diferente de `/perfil`, `/coach` **está** na lista de rotas privadas do proxy (não dá pra "recarregar /coach sem sessão" e ver o formulário, ele nem renderiza).
  PROVA (parte 2 — fetch cru sem sessão): `fetch('/api/coach', {method:'POST', body:{pergunta:'teste sem sessao'}})` sem cookie de sessão → `{"status":401,"body":{"erro":"Sessão ausente."}}` (mensagem crua do backend, `route.ts:27`).
  PROVA (parte 3 — fluxo real na UI): logado, entrei em `/coach`, removi o cookie `sb-tbkzcqfvafznxallyfqk-auth-token` via `document.cookie` (sem reload, simulando expiração no meio da sessão), digitei e enviei uma pergunta. `get_page_text` mostrou "Sessão expirada. Faça login novamente." exibida na tela. `read_network_requests` confirmou o `POST /api/coach` correspondente com `statusCode 401`. Confirma o mapeamento client-side em `coach-interativo.tsx:56-57` (`resposta.status === 401 ? "Sessão expirada..." : ...`), que traduz o "Sessão ausente." cru do backend pra mensagem amigável.
- [x] **COACH-07** Recarregar `/coach` (F5) depois de ter conversa na tela. Esperado: histórico **limpa** (não persiste — comportamento intencional, documentar que não é bug).
  PROVA: logado, enviei "Pergunta para teste de reload" em `/coach`, recebi resposta do coach ("Teste recebido com sucesso...", `get_page_text` confirmou as duas bolhas na tela). `navigate` pra `http://localhost:3002/coach` de novo (reload completo, equivalente a F5). `get_page_text` pós-reload voltou a mostrar só a mensagem inicial ("Pergunte o que quiser sobre treino...") — histórico realmente não persiste. Comportamento intencional (estado só em memória do componente), não é bug.
- [x] **COACH-08** Enviar 2-3 perguntas seguidas rapidamente (antes da resposta anterior chegar). Esperado: botão de enviar fica `disabled` enquanto `carregando`, impedindo múltiplas em voo — checar aba de rede pra confirmar que não há requisições sobrepostas.
  PROVA: digitei "Pergunta rapida 1" e cliquei em "Enviar" (`ref_3`) **3 vezes seguidas** sem esperar resposta. `read_network_requests` (filtro `api/coach`) mostrou apenas **1 nova requisição** `POST /api/coach` (200) correspondente a essa pergunta (os cliques extras não geraram novas requisições — bloqueados pelo `disabled`). `get_page_text` confirmou só 1 bolha do dono + 1 resposta do coach na tela, sem duplicação.
