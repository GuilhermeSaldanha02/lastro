# 09 — Transversais (aplicam a vários fluxos)

Rodar contra pelo menos 2 telas diferentes cada (indicado por item), não só uma — o ponto é confirmar que não é peculiaridade de uma tela só.

- [x] **TRANS-01** Voltar do navegador depois de fechar uma folha (`/ajustes/anilhas`, `(.)perfil`). Esperado: fecha a folha (não navega pra trás na pilha de páginas cheias) — comportamento de `router.back()`.
  PROVA: (1) `/ajustes` → cliquei "Anilhas" → URL virou `/ajustes/anilhas`, folha "Anilhas" sobreposta (screenshot, campo "Peso da barra" com 20, lista de anilhas). Em vez do X, usei `navigate({url:"back"})` (botão voltar do navegador) → resultado: URL `http://localhost:3002/ajustes` e a folha fechou, screenshot mostra a tela "Ajustes" cheia sem overlay — não pulou pra trás na pilha (ex. não caiu na Home). (2) `/ajustes` → cliquei "Editar perfil" → URL virou `/perfil` (rota interceptada `(.)perfil`), folha "Perfil" sobreposta (avatar vermelho, "Trocar foto"). `navigate({url:"back"})` → URL voltou a `http://localhost:3002/ajustes`, folha fechou, tela cheia "Ajustes" visível. Ambos os casos: 1 back = fecha só a folha, aterrissa na página cheia de baixo. Aprovado.
- [x] **TRANS-02** Voltar do navegador depois de salvar um modelo **dentro** da folha "criar modelo" (`AJU-20`). Esperado — regra específica de `DECISIONS.md` 2026-08-16 H1: não deveria empilhar rota nova, então voltar não deveria cair de novo na folha de criação. Confirmar.
  PROVA (reconfirmação ao vivo, já achado anteriormente em AJU-20 de `07-ajustes-anilhas-modelos.md`): `/ajustes/modelos` → "+ Criar modelo" → grupo "Peito" → passo 2, marquei "Crossover no cabo", nome "TRANS02 QA" → "Salvar modelo" → botão mostrou "Salvando..." e, após, URL final `http://localhost:3002/ajustes/modelos`, folha fechada, "TRANS02 QA" aparece na lista (screenshot). `navigate({url:"back"})` a partir daí → URL permaneceu `http://localhost:3002/ajustes/modelos` (lista, sem reabrir a folha "Novo modelo"), confirmando `router.back()`/substituição de rota em vez de `push`. Consistente com o achado prévio de AJU-20. Aprovado.
- [x] **TRANS-03** Recarregar (F5) no meio do formulário de registrar série, com campos parcialmente preenchidos. Esperado: perde o que não foi salvo (é esperado, formulário não persiste rascunho) — mas nenhuma série "fantasma" incompleta é criada no servidor.
  PROVA: treino extra criado pra QA (`/treino/f2352c9a-8d1c...e5e5`, "Treino em andamento", 17 ago). Já tinha 1 série "Abdominal infra 10×20 kg valendo" registrada. Cliquei "Outra série", preenchi Exercício "Abdominal infra" e Reps "15" — **sem** selecionar Tipo e **sem** preencher Peso (kg), formulário claramente incompleto (screenshot mostra campo Reps=15 com foco, Tipo ainda "Selecione o tipo", Peso vazio). Apertei `F5`. Resultado: página recarregou, formulário fechado (voltou ao estado padrão "Repetir última série"/"Outra série"), rascunho perdido como esperado. Contador continuou "1 valendo" e a única série listada continua "10×20 kg" — nenhuma série fantasma/incompleta foi criada. Aprovado.
- [x] **TRANS-04** Recarregar (F5) durante a confirmação inline de exclusão (série, treino ou modelo — testar pelo menos 2). Esperado: confirmação reseta, nada é excluído por acidente.
  PROVA (1, série): `/treino/f2352c9a-...` → "Editar" → lixeira da série "10×20 kg" → confirmação inline "Excluir a série 1 de Abdominal infra — 10×20 kg? Não dá pra desfazer." aberta (screenshot) → `F5` → após reload: confirmação sumiu, série "10×20 kg" ainda listada, "1 valendo" mantido. Nada excluído.
  PROVA (2, treino): `/treino` → "Editar" → lixeira do treino "17 ago" (o treino extra criado pra QA) → confirmação inline "Excluir o treino de 17 ago e a série dele? Não dá pra desfazer." aberta (screenshot) → `F5` → após reload: confirmação sumiu, treino "17 ago · 1 série" continua no histórico. Nada excluído. Aprovado nos 2 fluxos testados.
- [x] **TRANS-05** Duplo clique rápido no botão "Excluir" de uma confirmação (treino, modelo ou conta — os que têm `useTransition`/"Excluindo…"). Esperado: segunda ativação não dispara segunda exclusão (botão `disabled` durante `pendente`) — checar rede, 1 requisição só.
  PROVA: `/treino` → "Editar" → lixeira do treino "17 ago" → confirmação inline aberta → `computer double_click` no botão "Excluir" (dois cliques no mesmo ponto, o mais rápido que a ferramenta permite). Screenshot imediatamente após: botão mudou pra "Excluindo…" e "Cancelar" ficou acinzentado/desabilitado junto — evidência visual de estado `pendente` bloqueando nova interação. `read_network_requests` filtrado por `treino` mostrou só **1** requisição `POST http://localhost:3002/treino` (status 200) no fim da sequência — não 2. Resultado final: treino "17 ago" removido da lista uma única vez (lista voltou a mostrar só os 15 treinos originais da seed). Aprovado.
- [x] **TRANS-06** Duplo clique rápido em "Registrar série". Esperado: não cria 2 séries idênticas (checar se há alguma proteção — se não houver, é achado real, não suposição).
  PROVA: treino extra criado pra QA (`/treino/8c343031-d17b-...4e3f`). Preenchi exercício "Abdominal infra", tipo "Valendo", reps 12, peso 25 e dei `computer double_click` no botão "Registrar série" (2 cliques no mesmo ponto). Resultado: só **1** série foi criada ("12×25 kg", "1 valendo" — screenshot da lista). O 2º clique do duplo-clique não gerou 2ª série porque a tela já tinha trocado de layout no instante do 1º clique (volta pro resumo "Repetir última série"/"Outra série"), então o 2º clique caiu em cima do botão "Outra série" que apareceu na mesma posição — não resultou em nova requisição de registro. `read_network_requests` filtrado pelo id do treino mostrou só 1 POST correspondente ao registro em si. Sem série duplicada. Aprovado — proteção efetiva (ainda que via re-render/transição de tela em vez de um `disabled` explícito no botão, como em TRANS-05).
- [ ] **TRANS-07** REPROVADO (parcial) Navegação por teclado só (Tab/Shift+Tab/Enter/Espaço), sem mouse, em pelo menos 2 fluxos: registrar série completo, excluir um item com confirmação. Esperado: todo controle interativo alcançável, ordem de foco lógica, nenhum "buraco" onde o foco desaparece.
  FLUXO 1 (excluir com confirmação) — só teclado, sem mouse: `/ajustes/modelos` → `Tab` → foco em "Editar" → `Enter` → modo edição ativa ("Concluído" com foco) → `Tab` → foco no ícone lixeira de "TRANS02 QA" → `Enter` → confirmação inline "Excluir o modelo..." aparece → `Tab`, `Tab` → foco no botão "Excluir" (contorno azul visível) → `Enter` → modelo removido da lista (screenshot final mostra só "Biceps QA URL" restando). Ordem de foco lógica, nenhum buraco, tudo alcançável. OK.
  FLUXO 2 (registrar série completo) — achado real: a partir de `/treino/[id]`, `Tab` até "Adicionar exercício" (foco visível) → `Enter` abre a folha de seleção de grupo muscular, mas o foco **não é movido pra dentro da folha** (sem focus trap/auto-focus). `Tab` a partir daí pula direto pro rodapé de navegação inferior ("Início"→"Treinos"→...→"Ajustes"), depois cai num elemento invisível `NEXTJS-PORTAL` (confirmado via `document.activeElement`), e o ciclo completa voltando ao botão "<" no topo — **os checkboxes de grupo muscular (`Abdômen`, `Bíceps` etc.) nunca são alcançados só de Tab a partir do botão que abriu a folha**. Só depois de um clique de mouse dentro da folha o Tab passa a percorrer os chips normalmente (confirmado nesse ponto em diante: chips, "Continuar", "Exercício", "Tipo", "Reps", "Peso (kg)", checkbox "Peso corporal incluso" e "Registrar série" — todos com contorno de foco visível e alcançáveis em sequência lógica). Completei o registro por teclado a partir desse ponto (grupo "Abdômen" clicado manualmente → depois só teclado: exercício "Abdominal infra", tipo "Valendo", reps 10, peso 20 → `Registrar série` via `Enter`) e a série "10×20 kg — 1 valendo" foi criada com sucesso.
  CONCLUSÃO: achado real — "buraco" de foco confirmado ao abrir a folha "Adicionar exercício" (não documentado antes nesta auditoria). Restante do fluxo 2, a partir de dentro da folha, e todo o fluxo 1, passam sem buracos. Por isso o item fica reprovado (parcial), não aprovado nem não-coberto.
- [x] **TRANS-08** Foco visível (D9) em pelo menos 3 elementos distintos (botão, link, campo de formulário) — outline/contorno perceptível ao tabular, não removido por CSS.
  PROVA: `/ajustes`, cliquei no topo pra tirar foco do endereço e usei `Tab` real (tecla, não script): 1º Tab → cartão-link "qa-audit-geral2 / Editar perfil" com contorno azul nítido (screenshot). 2º Tab → cartão "Coach" com mesmo contorno azul. 3º Tab → cartão "Modelos" com contorno azul. Em `/ajustes/anilhas`, `Shift+Tab` a partir de "Editar" voltou ao campo de formulário "Peso da barra" (`input type=number`) com contorno azul e valor selecionado. 4 elementos distintos (3 links/cartões-de-ação + 1 campo de formulário), outline visível e não suprimido por CSS em nenhum. Aprovado.
- [NÃO COBERTO] **TRANS-09** Viewport 360×640 (piso realista) em pelo menos 3 telas densas: `/treino/[id]` com várias séries, `/analise` com parecer + gráfico, `/ajustes/anilhas` com grade de anilhas. Esperado: sem overflow horizontal, sem texto cortado, números não quebram no meio (regressão histórica de `.metrica__valor`/`.serie`, já corrigida — confirmar que continua corrigida).
  MOTIVO: `resize_window({width:360,height:640})` foi chamado, mas `window.innerWidth` real ficou travado em `500` (confirmado via `javascript_tool` em 3 tentativas — 390, 360 e 300px pedidos, sempre resultando em innerWidth=500) — piso de largura de janela do Chrome no Windows nesta extensão, sem forma disponível na ferramenta de contornar (zoom de página via teclado também bloqueado: "page zoom keyboard shortcuts are not supported"). Não dá pra simular 360px real de viewport.
  DADO SUPLEMENTAR (não substitui o item, é só o que deu pra checar no piso alcançável de 500px): nas 3 telas (`/treino/ac8113db-...`, `/analise`, `/ajustes/anilhas`) `document.documentElement.scrollWidth` (485) ficou sempre ≤ `window.innerWidth` (500) — sem overflow horizontal nesse piso, números ("12×20 kg", "20 kg") não quebraram. Não é evidência suficiente pra 360px, só descarta regressão grosseira.
- [x] **TRANS-10** Alvo de toque mínimo 48×48px (D1) — medir pelo menos 3 botões/links de ação primária via inspeção de CSS computado, não só olhar.
  PROVA: em `/ajustes/anilhas`, via `javascript_tool` com `getBoundingClientRect()` + `getComputedStyle()` sobre `nav a, button`: link "Editar" → `{w:55.47, h:48, minHeight:"48px"}`; botão "+ Adicionar" → `{w:216.5, h:48, minHeight:"48px"}`; botão "Salvar configuração" → `{w:445, h:72, minHeight:"72px"}`; itens de navegação inferior "Início"/"Treinos"/"Análise"/"Catálogo"/"Ajustes" → todos `{w:87.4, h:48, minHeight:"48px"}`. Todos os 3+ elementos medidos ficam em 48px ou mais nas duas dimensões. Aprovado.
- [x] **TRANS-11** Erro de rede genérico simulado onde a UI trata explicitamente (ex.: `/analise`, `/coach` — bloquear a requisição via alguma forma disponível na ferramenta, ou provocar timeout). Esperado: mensagens específicas mapeadas em `06-analise-progressao.md`/`08-perfil-coach.md`, nunca uma tela em branco ou erro não tratado do React (checar console — zero erro não capturado).
  MÉTODO: sem controle de offline/throttle na ferramenta de navegador; simulei erro de rede via `javascript_tool` sobrescrevendo `window.fetch` para rejeitar (`Promise.reject(new TypeError('Failed to fetch'))`) qualquer requisição cuja URL contivesse `analise`/`coach`/`api`, antes de disparar a ação — efeito equivalente a queda de rede pro código da aplicação, que só vê a Promise rejeitada.
  PROVA (1, `/analise`): override instalado → cliquei "Solicitar Análise" → `get_page_text` mostrou card vermelho "Falha de rede ao gerar o parecer. Tente novamente." logo abaixo das perguntas-sugestão (screenshot confirma card com borda vermelha). `read_console_messages({onlyErrors:true})` → "No console errors or exceptions found for this tab." Nenhuma tela em branco, nenhum erro React não tratado.
  PROVA (2, `/coach`): navegação para `/coach` reseta o contexto JS (override se perde — confirmado via `window.fetch === window.__origFetch`), reinstalei o override → digitei "TRANS-11 QA teste de erro de rede" no campo "Perguntar…" → cliquei "Enviar" → `get_page_text`/screenshot mostram a pergunta ecoada como bolha verde e, logo abaixo, card vermelho "Falha de rede. Tente de novo." `read_console_messages({onlyErrors:true})` → nenhum erro. Ambos os fluxos tratam a falha explicitamente com mensagem específica, sem tela em branco e console limpo. Aprovado.
- [x] **TRANS-12** Sessão expirada no meio de uma ação de rede (ex.: deletar cookie de sessão manualmente entre abrir a tela e clicar "Solicitar Análise" ou enviar pergunta no coach). Esperado: mensagem "Sessão expirada. Faça login novamente." nos 2 fluxos que tratam 401 explicitamente.
  MÉTODO: com a tela já carregada e sessão válida, apaguei o cookie `sb-tbkzcqfvafznxallyfqk-auth-token` via `javascript_tool` (`document.cookie = nome+'=; expires=Thu, 01 Jan 1970...; path=/;'`, confirmando `document.cookie` vazio depois) SEM recarregar a página — simula exatamente "sessão cai no meio da ação", já que um F5/navegação completa dispara redirect server-side pra `/login` antes mesmo de testar o tratamento client-side do 401.
  PROVA (1, `/analise`): em `/analise` (sessão válida, "Semana fechada" visível) → apaguei o cookie → cliquei "Solicitar Análise" (sem reload) → `get_page_text` mostrou card vermelho "Sessão expirada. Faça login novamente." abaixo das perguntas-sugestão (screenshot confirma). `read_console_messages({onlyErrors:true})` → nenhum erro.
  PROVA (2, `/coach`): descoberta no processo — navegar (full navigation) para `/coach` com o cookie já apagado redireciona server-side pra `/login?proximo=%2Fcoach` (auth checada no servidor), então não dá pra testar o tratamento client-side dessa forma. Refiz: login novo (usuário de teste) → app client-side já em `/coach` com sessão válida → apaguei o cookie via `javascript_tool` sem navegar → digitei "TRANS-12 QA sessao expirada" → cliquei "Enviar" → `get_page_text`/screenshot mostram a pergunta ecoada (bolha verde) e, abaixo, card vermelho "Sessão expirada. Faça login novamente." `read_console_messages({onlyErrors:true})` → nenhum erro. Mensagem exata esperada nos 2 fluxos, sem erro não tratado. Aprovado.
- [x] **TRANS-13** Console do navegador limpo (sem erro/warning novo introduzido) em pelo menos 5 telas visitadas nesta auditoria — colar a saída crua mesmo quando vazia, é parte da prova, não só quando há erro.
  MÉTODO: login novo (usuário de teste, sessão tinha expirado de propósito em TRANS-12) → naveguei (full navigation, garantindo listener de console reiniciado por página) por 5 telas em sequência, aguardando ~1s de carregamento em cada, e rodei `read_console_messages({onlyErrors:true})` logo em seguida. Saída crua de cada tela:
  1. `/` → "No console errors or exceptions found for this tab."
  2. `/treino` → "No console errors or exceptions found for this tab."
  3. `/analise` → "No console errors or exceptions found for this tab."
  4. `/catalogo` → "No console errors or exceptions found for this tab."
  5. `/ajustes` → "No console errors or exceptions found for this tab."
  Nenhum erro ou warning capturado em nenhuma das 5 telas. Aprovado.
