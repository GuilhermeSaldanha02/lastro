// lastro · slot paralelo do modal (H1) — nada renderiza aqui fora de uma
// navegação interceptada. Obrigatório: sem isto, o Next não sabe o que
// mostrar no slot em toda rota que não seja `(.)perfil`.
export default function ModalPadrao() {
  return null;
}
